{
  //是提醒级别，如果为error则会报错，如果为warning则会警告，如果设为off则关闭，那TSLint就关闭了；
  "defaultSeverity": "error",
  //可指定继承指定的预设配置规则；
  "extends": [
    "tslint:recommended"
  ],
  // jsRules用来配置对.js和.jsx文件的校验，配置规则的方法和下面的rules一样；
  "jsRules": {},
  // 让TSLint根据怎样的规则来检查代码，都是在这个里面配置，比如当我们不允许代码中使用eval方法时，就要在这里配置"no-eval": true,
  "rules": {},
  // 可以指定规则配置文件，这里指定相对路径。
  "rulesDirectory": []
}